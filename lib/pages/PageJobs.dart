import 'package:flutter/material.dart';

class PageJobs extends StatelessWidget {
  const PageJobs({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Empleos", style: TextStyle(fontSize: 30),),
    );
  }
}