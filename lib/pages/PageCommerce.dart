import 'package:flutter/material.dart';

class PageCommerce extends StatelessWidget {
  const PageCommerce({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Comercios", style: TextStyle(fontSize: 30),),
    );
  }
}